<!-- added "semantic_" to line below #00425 -->
<%= semantic_form_for(@activity_type) do |f| %>
    
  
  <% if @activity_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_type.errors.count, "error") %> prohibited this activity_type from being saved:</h2>

      <ul>
      <% @activity_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- original code before #00425 -->
  <% if false %> 
        <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>

  <!-- New code for #00425 -->
  <!-- <%= f.semantic_errors %> -->
  <div class="field">
    <%= f.inputs do %>
      <%= f.input :name,   :hint => "Enter the activity type name" %>
    <% end %>
  </div>
  <div class="actions">
    <%= f.actions do %>
      <%= f.action :submit %>
    <% end %>
  </div>
  
<% end %>
